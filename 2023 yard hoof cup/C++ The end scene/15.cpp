#include <bits/stdc++.h>

using namespace std;
#define int long long

void solve() {
}

auto main() -> signed {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);

    solve();
    return 0;
}
